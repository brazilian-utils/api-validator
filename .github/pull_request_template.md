## What changes

<!-- New function, new test vector (bug fix / decision), naming alias, lib config, tooling... -->

## Contract changes

- [ ] Every new or changed function has test vectors (a bug fix adds the vector that exposes it)
- [ ] Behaviour questions are settled by a vector, with a `note` explaining the decision
- [ ] `npm run lint` passes (contract valid and formatted: `npx tsx src/cli.ts fmt`)
- [ ] The Conformance check on this PR shows which libs are affected (no baseline regressions)

## Libs affected

<!-- From the Conformance job summary: which libs now miss this / fail these vectors. They get
it in their `api-contract` issue once this is merged. -->
