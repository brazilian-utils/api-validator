{
  "$schema": "../../schema/contract.schema.json",
  "domain": "ncm",
  "title": { "en": "NCM", "pt-BR": "NCM" },
  "summary": {
    "en": "Nomenclatura Comum do Mercosul: the goods classification codes published by Siscomex.",
    "pt-BR": "Nomenclatura Comum do Mercosul: os códigos de classificação de mercadorias publicados pelo Siscomex."
  },
  "category": "tax",
  "order": 3,
  "functions": {
    "format": {
      "summary": { "en": "Formats an NCM code.", "pt-BR": "Formata um código NCM." },
      "description": {
        "en": [
          "Formats an NCM code with the mask `NNNN.NN.NN`. Only the structure changes (use `ncm.isValid` to check the code).",
          "",
          "- Same rules as `cnae.format`. `options.pad` first left-pads the value with zeros to 8 digits."
        ],
        "pt-BR": [
          "Formata um código NCM com a máscara `NNNN.NN.NN`. Só a estrutura muda (use `ncm.isValid` para verificar o código).",
          "",
          "- Mesmas regras de `cnae.format`. `options.pad` antes completa o valor com zeros à esquerda até 8 dígitos."
        ]
      },
      "references": ["https://portalunico.siscomex.gov.br/classif/api/publico/nomenclatura/download/json"],
      "params": [
        { "name": "value", "type": "string | number" },
        { "name": "options", "type": "FormatNcmOptions", "optional": true }
      ],
      "returns": "string",
      "tests": [
        { "args": ["84713012"], "returns": "8471.30.12" },
        { "name": "already-formatted", "args": ["8471.30.12"], "returns": "8471.30.12" },
        { "name": "partial", "args": ["84713"], "returns": "8471.3" },
        { "name": "empty", "args": [""], "returns": "" },
        { "args": [84713012], "returns": "8471.30.12", "note": "JavaScript's own test: should format an NCM code given as a number" },
        { "args": ["8"], "returns": "8", "note": "JavaScript's own test: should mask a partial value progressively by default" },
        { "args": ["84"], "returns": "84", "note": "JavaScript's own test: should mask a partial value progressively by default" },
        { "args": ["847"], "returns": "847", "note": "JavaScript's own test: should mask a partial value progressively by default" },
        { "args": ["8471"], "returns": "8471", "note": "JavaScript's own test: should mask a partial value progressively by default" },
        { "args": ["847130"], "returns": "8471.30", "note": "JavaScript's own test: should mask a partial value progressively by default" },
        { "args": ["8471301"], "returns": "8471.30.1", "note": "JavaScript's own test: should mask a partial value progressively by default" },
        { "args": [8471], "returns": "8471", "note": "JavaScript's own test: should mask a partial number progressively by default" },
        { "args": [847130], "returns": "8471.30", "note": "JavaScript's own test: should mask a partial number progressively by default" },
        { "args": ["00000000"], "returns": "0000.00.00", "note": "JavaScript's own test: should not validate whether the code exists in the official table" },
        { "args": ["847130120000"], "returns": "8471.30.12", "note": "JavaScript's own test: should not add digits after the NCM length" },
        { "args": ["", { "pad": true }], "returns": "0000.00.00", "note": "JavaScript's own test: pad option should left pad a short code with zeros up to the full NCM length" },
        { "args": ["1", { "pad": true }], "returns": "0000.00.01", "note": "JavaScript's own test: pad option should left pad a short code with zeros up to the full NCM length" },
        { "args": ["8471", { "pad": true }], "returns": "0000.84.71", "note": "JavaScript's own test: pad option should left pad a short code with zeros up to the full NCM length" },
        { "args": ["847130", { "pad": true }], "returns": "0084.71.30", "note": "JavaScript's own test: pad option should left pad a short code with zeros up to the full NCM length" },
        { "args": ["84713012", { "pad": true }], "returns": "8471.30.12", "note": "JavaScript's own test: pad option should left pad a short code with zeros up to the full NCM length" },
        { "args": [8471, { "pad": true }], "returns": "0000.84.71", "note": "JavaScript's own test: pad option should left pad a number the same way as its digits" },
        { "args": [84713012, { "pad": true }], "returns": "8471.30.12", "note": "JavaScript's own test: pad option should left pad a number the same way as its digits" },
        { "args": ["8471", { "pad": false }], "returns": "8471", "note": "JavaScript's own test: pad option should mask progressively for an explicit false" },
        { "args": ["abc8471"], "returns": "8471", "note": "JavaScript's own test: should read only the digits of a value with other characters, like formatCpf" },
        { "args": ["8471.30-12"], "returns": "8471.30.12", "note": "JavaScript's own test: should read only the digits of a value with other characters, like formatCpf" },
        { "args": [-84713012], "returns": "8471.30.12", "note": "JavaScript's own test: should read a signed or fractional number as the string of its digits, like formatCpf" },
        { "args": [8471301.2], "returns": "8471.30.12", "note": "JavaScript's own test: should read a signed or fractional number as the string of its digits, like formatCpf" }
      ]
    },
    "isValid": {
      "summary": {
        "en": "Checks whether an NCM code exists in the official table.",
        "pt-BR": "Verifica se um código NCM existe na tabela oficial."
      },
      "description": {
        "en": [
          "Checks whether an NCM code exists in the current table that Siscomex publishes.",
          "",
          "- Same input rules as `cbo.isValid`, with 8 digits and the `NNNN.NN.NN` mask."
        ],
        "pt-BR": [
          "Verifica se um código NCM existe na tabela vigente publicada pelo Siscomex.",
          "",
          "- Mesmas regras de entrada de `cbo.isValid`, com 8 dígitos e a máscara `NNNN.NN.NN`."
        ]
      },
      "references": ["https://portalunico.siscomex.gov.br/classif/api/publico/nomenclatura/download/json"],
      "params": [{ "name": "value", "type": "string | number" }],
      "returns": "boolean",
      "tests": [
        { "args": ["22030000"], "returns": true },
        { "name": "masked", "args": ["2203.00.00"], "returns": true },
        { "name": "leading-zero", "args": ["01012100"], "returns": true },
        { "name": "nonexistent", "args": ["12345678"], "returns": false },
        { "name": "too-short", "args": ["2203000"], "returns": false },
        { "name": "letters", "args": ["abcdefgh"], "returns": false },
        { "name": "empty", "args": [""], "returns": false },
        { "args": [22030000], "returns": true, "note": "JavaScript's own test: should validate an NCM code given as a number" },
        { "args": ["0101.21.00"], "returns": true, "note": "JavaScript's own test: should validate a leading zero NCM code (cavalos reprodutores de raça pura)" },
        { "args": [1012100], "returns": true, "note": "JavaScript's own test: should pad a value to eight digits, as a number or as a string (1012100 is 01012100)" },
        { "args": ["1012100"], "returns": true, "note": "JavaScript's own test: should pad a value to eight digits, as a number or as a string (1012100 is 01012100)" },
        { "args": ["101.21.00"], "returns": false, "note": "JavaScript's own test: should not pad a masked value, which already carries its separators" },
        { "args": [" 22030000 "], "returns": true, "note": "JavaScript's own test: should validate an NCM code with surrounding whitespace" },
        { "args": ["220300000"], "returns": false, "note": "JavaScript's own test: should return false for a padded short value no code carries and for a wider value" },
        { "args": ["        "], "returns": false, "note": "JavaScript's own test: should return false for whitespace only" },
        { "args": ["abc01012100"], "returns": false, "note": "JavaScript's own test: should return false for a string that is not a documented form" },
        { "args": ["2203..00.00"], "returns": false, "note": "JavaScript's own test: should return false for a string that is not a documented form" },
        { "args": [-22030000], "returns": false, "note": "JavaScript's own test: should return false for a number that is not a non-negative safe integer" },
        { "args": [2203000.01], "returns": false, "note": "JavaScript's own test: should return false for a number that is not a non-negative safe integer" }
      ]
    },
    "parse": {
      "summary": {
        "en": "Removes the formatting characters of an NCM code and returns only the digits.",
        "pt-BR": "Remove os caracteres de formatação de um código NCM e retorna só os dígitos."
      },
      "description": {
        "en": [
          "Removes NCM formatting and keeps only digits, capped at 8 digits.",
          "",
          "- The function does not left-pad the value. It keeps a partial code as written."
        ],
        "pt-BR": [
          "Remove a formatação do NCM e mantém apenas os dígitos, limitados a 8.",
          "",
          "- A função não completa o valor com zeros à esquerda. Ela mantém um código parcial como foi escrito."
        ]
      },
      "references": ["https://portalunico.siscomex.gov.br/classif/api/publico/nomenclatura/download/json"],
      "params": [{ "name": "value", "type": "string | number" }],
      "returns": "string",
      "tests": [
        { "name": "masked", "args": ["8471.30.12"], "returns": "84713012" },
        { "name": "unmasked", "args": ["84713012"], "returns": "84713012" },
        { "name": "strips-non-digits", "args": ["84?ABC71.30.12abc"], "returns": "84713012" },
        { "name": "empty", "args": [""], "returns": "" },
        { "name": "caps-length", "args": ["84713012999"], "returns": "84713012", "note": "reference truncates to the 8 characters of the document; asserted by the reference (JS) unit tests" },
        { "args": [84713012], "returns": "84713012", "note": "JavaScript's own test: should read a number as the string of its digits" },
        { "args": ["8471"], "returns": "8471", "note": "JavaScript's own test: should keep a partial code as written, without padding it" }
      ]
    }
  }
}
