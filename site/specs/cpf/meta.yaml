# Site metadata for this spec. The rule itself lives in spec.md / spec_en.md.
category: documents
order: 1
title:
  en: CPF
  pt-BR: CPF
summary:
  en: Cadastro de Pessoas Físicas, the 11-digit Brazilian individual taxpayer number, ending in two mod-11 check digits.
  pt-BR: Cadastro de Pessoas Físicas, o número de 11 dígitos do contribuinte pessoa física, terminado em dois dígitos verificadores por módulo 11.

# Operation ids are the `## headings` each library uses in its docs/usage/cpf.md file.
operations:
  - id: validate
    label: { en: Validate, pt-BR: Validar }
  - id: format
    label: { en: Format, pt-BR: Formatar }
  - id: remove-symbols
    label: { en: Remove symbols, pt-BR: Remover símbolos }
  - id: generate
    label: { en: Generate, pt-BR: Gerar }

related: [cnpj]
